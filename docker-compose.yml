
services:
  web:
    container_name: web
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - 8000:8000
    env_file:
      - ./.env.dev
    volumes:
      - media_files:/usr/src/app/media

volumes:
  media_files:
