
services:
  web:
    container_name: web
    build: .
    ports:
      - 8000:8000
    env_file:
      - ./.env.dev
    volumes:
      - media_files:/usr/src/app/media

  redis:
    image: redis:7-alpine

volumes:
  media_files:
